[package]
name = "build-helpers"
version.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
sha2 = "0.11"

[dev-dependencies]
tempfile = "3"
# Reads the tree's manifests in tests/sealed_tree.rs. Parsing only: the suite
# never writes a manifest back.
toml_edit = { version = "0.25", default-features = false, features = ["parse"] }

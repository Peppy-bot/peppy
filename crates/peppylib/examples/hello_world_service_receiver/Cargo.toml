[package]
name = "hello_world_service_receiver"
version = "0.1.0"
edition = "2024"

[dependencies]
peppylib = { path = "../.." }
config = { path = "../../../config-internal" }
tokio = { version = "1.47.0", features = ["rt-multi-thread", "macros"] }
chrono = { version = "0.4" }
names-generator2 = "0.2.0"
bytes = "1"

[workspace]

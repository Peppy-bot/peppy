[package]
name = "hello_world_topic_receiver"
version = "0.1.0"
edition = "2024"

[dependencies]
peppylib = { path = "../.." }
config = { path = "../../../config-internal" }
tokio = { version = "1.47.0", features = ["rt-multi-thread", "macros"] }

[workspace]

[package]
name = "hello_world_action_sender"
version = "0.1.0"
edition = "2024"

[dependencies]
peppylib = { path = "../.." }
config = { path = "../../../config-internal" }
tokio = { version = "1.47.0", features = ["rt-multi-thread", "macros"] }
bytes = "1.10.1"
colored = "3"

[workspace]

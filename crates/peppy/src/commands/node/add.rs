use std::path::PathBuf;

pub fn add_node(peppy_json5: PathBuf, run: bool) {
    // TODO Invoke the `add` service on the master_node and check that the node has been added to the node stack. If `run` is true, run the `start` service on the master node and print out the returned instance id
}

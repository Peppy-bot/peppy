#[test]
fn node_add_command_succeeds() {
    todo!(
        "Spawn a node by invoking the `add` on the master_node service and check that the node has been added to the node stack"
    )
}

#[test]
fn node_add_command_with_run_arg_succeeds() {
    todo!(
        "Spawn a node by invoking the `add` on the master_node service and then the `start` service to start it immediately and check that the node has been added to the node stack and an instance has started"
    )
}

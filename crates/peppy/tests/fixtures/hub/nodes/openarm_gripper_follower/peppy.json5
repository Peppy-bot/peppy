{
  peppy_schema: "node/v1",
  manifest: {
    name: "openarm_gripper_follower",
    tag: "v1",
    depends_on: {
      // One limb of a teleoperated pair, playing the `follower` role of
      // `gripper_link/v1` through its single participant slot.
      pairings: [
        { name: "gripper_link", tag: "v1", role: "follower", link_id: "limb" }
      ]
    },
    labels: ["follower"]
  },
  execution: {
    language: "rust",
    run_cmd: ["./bin/openarm_gripper_follower"]
  },
  interfaces: {
    topics: {
      emits: [
        { link_id: "limb", name: "gripper_states" }
      ],
      consumes: [
        { link_id: "limb", name: "gripper_setpoints" }
      ]
    }
  }
}

#[test]
fn node_list_command_succeeds() {
    todo!(
        "Run a node add command with `run == true` to add a node to the master node node stack then call the `list` service on the master node which will give back a DOT representation. Check that the node and its instance are present in the DOT representation"
    )
}

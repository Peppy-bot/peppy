{
  peppy_schema: "node/v1",
  manifest: {
    name: "{{ node_name }}",
    tag: "v1",
  },
  interfaces: {},
{% if with_container %}
  execution: {
    language: "rust",
    container: {
      def_file: "apptainer.def",
    },
  },
{% else %}
  execution: {
    language: "rust",
    build_cmd: [
      "cargo",
      "build",
      "--release"
    ],
    run_cmd: [
      "./target/release/{{ node_name }}"
    ]
  },
{% endif %}
}

Bootstrap: docker
From: {{ base_image }}

%labels
    Name {{ node_name }}
    Version {{ tag }}

%files
    . /opt/{{ node_name }}

%post
    set -eux

    cd /opt/{{ node_name }}
    cargo build --release

%runscript
    cd /opt/{{ node_name }}
    exec ./target/release/{{ node_name }}

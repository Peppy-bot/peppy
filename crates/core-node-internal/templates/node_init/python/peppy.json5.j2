{
  schema_version: 1,
  manifest: {
    name: "{{ node_name }}",
    tag: "0.1.0",
  },
{% if with_container %}
  runtime: {
    language: "python",
    container: {
      def_file: "apptainer.def",
    },
  },
{% else %}
  runtime: {
    language: "python",
    add_cmd: [
      "uv",
      "sync",
      "--no-editable"
    ],
    start_cmd: [
      "./.venv/bin/python",
      "-m",
      "{{ node_name }}"
    ]
  },
{% endif %}
  interfaces: {}
}

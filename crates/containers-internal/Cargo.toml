[package]
name = "containers"
version.workspace = true
authors.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2.0"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

[build-dependencies]
sha2 = "0.10"

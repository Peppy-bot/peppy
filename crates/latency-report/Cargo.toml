[package]
name = "latency-report"
version.workspace = true
authors.workspace = true
edition.workspace = true

# Pure, std-only leaf crate: percentile/summary math, duration/table formatting,
# the machine-id-keyed baseline file, and CPU-turbo environment detection.
# Shared by the offline latency bench (generator) and `peppy stack benchmark`.
[dependencies]

[project]
name = "peppycl"
version = "0.1.0"
description = "Peppy client library for Python"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"
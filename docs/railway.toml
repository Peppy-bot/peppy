[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
buildCommand = "npm run build"

[deploy]
startCommand = "npx astro preview --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300

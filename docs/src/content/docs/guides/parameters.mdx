---
title: Node parameters
description: Learn how to pass starting parameters to nodes
sidebar:
  order: 5
---
import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import helloWorldParamRust from './snippets/rust/hello_world_param/src/main.rs?raw';
import helloWorldParamConfig from './snippets/rust/hello_world_param/peppy.json5?raw';

Sometimes you may want to run multiple instances of the same node with different input parameters. For example, a camera node might point to `/dev/video0` on your system while another one points to `/dev/video1`.
Parameters allow you to run multiple instances of the same node with different input settings.

## Adding parameters to `hello_world`

Modify the `peppy.json5` configuration of our hello_world node to look like this:

<Code code={helloWorldParamConfig} lang="json5" title="peppy.json5" />

With the new `parameter` key.

## Synchronizing the interfaces

Now let's try to create a new snapshot of our node in the node stack:
```sh
peppy node add .
```

You'll get an error similar to this one:
```text
Error: Fingerprint verification failed: Node config fingerprint mismatch: expected 9a58e1936c52c83d47d31621108e32bb78d2197c2d1989f0001e945a754e29a8, got ca1b1d51711a782db3152a723c6cdc255191c2a1721c7a19088e878fc9879c39. 
The config may have been modified after code generation. Run `node sync` to update the peppygen lib on your node.
```

The reason for this error is that changes in `peppy.json5` must first be reflected in the interfaces. This protection ensures that the `peppy.json5` **always** matches the actual behavior of the node.
To sync the node, run:
```sh
peppy node sync
```
Now try to add the node again:
```sh
peppy node add .
```
The command should work this time, and you can see the node in the node stack by running:
```sh
peppy stack list
```

## Using the parameter

Now that we've synchronized our project, the new parameter can be used in the main file by modifying it like this:

<Tabs>
  <TabItem label="Rust">
    <Code code={helloWorldParamRust} lang="rust" title="src/main.rs" />
  </TabItem>
  <TabItem label="Python">
    :::caution
    The Python toolchain is not yet available.
    :::
  </TabItem>
</Tabs>

Now add the node to the stack. Since we renamed the node to `hello_world_param:0.1.0` in `peppy.json5`, peppy treats it as a new node:
```sh
peppy node add .
```

While we're at it, let's remove the old `hello_world:0.1.0` node that was previously created in this folder:
```sh
peppy node remove hello_world:0.1.0
```

:::note
You can overwrite nodes in the stack that share the same name and tag by running `peppy node add` again, as long as those nodes have no dependencies. This simplifies the development workflow.
:::
pub struct TestServeHandle {}

impl TestServeHandle {
    pub fn start() -> Self {
        todo!("Call `peppy service serve` while passing a random port to it")
    }

    pub fn port() -> u16 {
        todo!("Finish")
    }
}

---
tags:
  - node
---

Creates a new node in the current directory. This command requires the presence of a `peppy.star` configuration file in the directory where it's executed.

Possible arguments are:
 - `--lang`: Either `rust` or `python`, defaults to `rust` (might change in future versions)
 - 
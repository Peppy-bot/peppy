name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: self-hosted
    container: rust:latest

    steps:
      - uses: actions/checkout@v6

      - name: Run tests
        run: cargo test --verbose

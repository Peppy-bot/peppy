name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, dev]

concurrency:
  group: tests-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  # CI does the full cross-platform peppylib build: cross-compile the .so for
  # every Linux arch. A plain `cargo build` leaves this unset and, like a Linux
  # build, produces only the host dynamic lib, so local dev builds stay fast; the
  # release path and CI turn it on. On a warm build cache this is a no-op (present,
  # fresh artifacts are reused); it only pays the cross-build cost when the cache
  # is cold. Cross-arch apptainer is separate, gated by PEPPY_CROSS_ARCH.
  PEPPY_CROSS_BUILD: "1"

jobs:
  test:
    runs-on: blacksmith-16vcpu-ubuntu-2404
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # Nothing in this job pushes, so it has no use for a writable
          # credential sitting in .git/config for every later step to inherit.
          persist-credentials: false

      # Which of the later gated suites have had their inputs touched. The
      # three filters below mirror the source sets of their suites: the
      # crates each cargo command compiles from this workspace (the internal
      # path-dependency closure from the resolve graph, including dev and
      # build dependencies), plus the repo files those steps read and the
      # build inputs every cargo step shares. Keep each list in sync with its
      # command's dependencies: when a crate gains or drops a workspace
      # dependency, its directory moves between these lists too.
      - name: Detect changed test inputs
        id: changes
        uses: dorny/paths-filter@v4
        with:
          filters: |
            container_e2e:
              - 'crates/core-node-internal/**'
              - 'crates/containers-internal/**'
              - 'crates/daemon-config-internal/**'
              - 'crates/encoding-internal/**'
              - 'crates/generator-internal/**'
              - 'crates/latency-report/**'
              - 'crates/mcp-test-support/**'
              - 'crates/node-stack-internal/**'
              - 'crates/peppylib-build-policy/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.cargo/config.toml'
              - '.github/actions/rust-build-env/**'
              - '.github/workflows/tests.yml'
            docs_integration:
              # The suite also walks the docs snippets at runtime, which live
              # outside this package's directory.
              - 'docs/tests/integration/**'
              - 'docs/src/content/docs/guides/snippets/**'
              - 'crates/auth-internal/**'
              - 'crates/containers-internal/**'
              - 'crates/core-node-internal/**'
              - 'crates/daemon-config-internal/**'
              - 'crates/daemon-internal/**'
              - 'crates/encoding-internal/**'
              - 'crates/generator-internal/**'
              - 'crates/latency-report/**'
              - 'crates/mcp-test-support/**'
              - 'crates/node-stack-internal/**'
              - 'crates/peppy/**'
              - 'crates/peppylib-build-policy/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.cargo/config.toml'
              - '.github/actions/rust-build-env/**'
              - '.github/workflows/tests.yml'
            scripts:
              # The release scripts suite is self-contained in scripts/,
              # toolchain included (pixi.toml, pixi.lock, pyproject.toml).
              - 'scripts/**'

      # Give this run its own peppy data root so the runner does not accumulate
      # or collide on /tmp/.peppy across runs. Only the Rust cargo steps below
      # opt in via env; the release-scripts step is left alone so
      # scripts/install.sh keeps defaulting PEPPY_HOME to $HOME/.peppy.
      - name: Set up isolated peppy data root
        run: |
          PEPPY_RUN_HOME="$RUNNER_TEMP/peppy-home"
          mkdir -p "$PEPPY_RUN_HOME/tmpdir"
          echo "PEPPY_RUN_HOME=$PEPPY_RUN_HOME" >> "$GITHUB_ENV"

      # Rust toolchain plus the system libraries the container build script
      # compiles apptainer against. Must precede every cargo step.
      - uses: ./.github/actions/rust-build-env

      # A persistent Docker builder: setup-docker-builder starts a buildkitd
      # whose layer cache lives on a sticky disk, hydrated from this job's
      # previous runs and committed back when the job succeeds, and makes it
      # the default builder for every later `docker buildx` command. The
      # multi-daemon e2e builds its daemon image through that client, so its
      # apt and uv layers are built once and reused across runs; the Docker
      # daemon's own embedded builder would rebuild them from scratch on
      # every ephemeral runner. One cache key per build workload, per
      # Blacksmith's scoping guidance.
      - name: Set up the cached Docker builder
        uses: useblacksmith/setup-docker-builder@v2
        with:
          cache-key: peppy/multi-daemon-e2e-image

      - name: Run tests
        run: |
          export PEPPY_HOME="$PEPPY_RUN_HOME" TMPDIR="$PEPPY_RUN_HOME/tmpdir"
          cargo test --locked

      - name: Run container e2e tests
        if: steps.changes.outputs.container_e2e == 'true'
        run: |
          export PEPPY_HOME="$PEPPY_RUN_HOME" TMPDIR="$PEPPY_RUN_HOME/tmpdir"
          cargo test --locked -p core-node --features container_e2e --test container_e2e

      - name: Run multi-daemon Docker e2e test
        run: |
          export PEPPY_HOME="$PEPPY_RUN_HOME" TMPDIR="$PEPPY_RUN_HOME/tmpdir"
          cargo test --locked -p peppy --features multi_daemon_e2e --test multi_daemon_e2e

      - name: Run documentation integration tests
        if: steps.changes.outputs.docs_integration == 'true'
        run: |
          export PEPPY_HOME="$PEPPY_RUN_HOME" TMPDIR="$PEPPY_RUN_HOME/tmpdir"
          cargo test --locked -p docs-integration-tests

      - name: Run release scripts tests
        if: |
          (github.event_name == 'pull_request' && github.base_ref == 'main')
          || steps.changes.outputs.scripts == 'true'
        run: ./scripts/run_tests.sh --all

      # Proves the build consumed the committed lockfile rather than re-resolving
      # it, so the tested dependency graph is the shipped one. Must stay after the
      # cargo steps: placed before them it proves nothing.
      - name: Prove CI did not rewrite the shipping lockfile
        run: git diff --exit-code -- Cargo.lock

      # Reclaim the per-run data root so disk does not grow on every run. Runs
      # even when a test step failed. The persistent build-tool cache at
      # $HOME/.peppy/tmp is intentionally left in place (see build_helpers::cache_dir).
      - name: Clean up isolated peppy data root
        if: always()
        run: rm -rf "$RUNNER_TEMP/peppy-home"
